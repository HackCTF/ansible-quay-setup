- name: Configuración de Quay en múltiples servidores
  hosts: quay_servers
  become: yes
  vars_files:
    - vars/global.yml
  vars:
    project_path: "/opt/quay"
    ip_host: "192.168.1.70"

  roles:
    - config
    - postgres
    - quay_login
